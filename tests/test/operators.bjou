# operators.bjou

proc p(s : char*) { }

proc main() {
	1 + 1
	3.0 - 1.23
	# (2 as float) * 4.0
	2 * 4.0
	6 / 4
	10 % 2

	i : int = 4 + 2 * 1

	s := "string"
	s += i - 2
	i -= 1
	s -= i - 3
	i *= 2
	i /= 2
	i %= 2
	print "%", i
# i ?? some 12345
	u : u32 = 1
	b : bool
	# b = 123 < u 	warning
	b = i <= 9
	b = i > 0
	b = i >= 1
	b = i == 42
	b = i != 0
	print "i = %, u = %, b = %", i, u, b
}

main()
