# demo2.bjou
# Constants

(proc main() {
    const PI : float = 355.0 / 113.0
    print "pi = %", PI
    pi := PI
    const TWO_PI := 2.0 * PI
    print "2 x % = %", PI, TWO_PI
    i := 1
    # const ZERO := i - 1 # not a constant expression -- will not compile
})()
